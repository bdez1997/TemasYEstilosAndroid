<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PracticeRestart" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="btnNormal" parent="Theme.PracticeRestart">
        <item name="android:width">@dimen/anchoBtn</item>
        <item name="android:height">@dimen/altoBtn</item>
    </style>
    <style name="lblError" parent="Theme.PracticeRestart">
        <item name="android:textColor">@color/lblError</item>
        <item name="android:textSize">@dimen/tamanioTexto</item>
    </style>
    <style name="rdioBtn" parent="Theme.PracticeRestart">
        <item name="android:textColor">@color/radioBtnText</item>
        <item name="android:color">@color/radioBtn</item>
        <item name="buttonTint">@color/radioBtnColorLink</item>

    </style>
</resources>